## Codex Automation

When the user asks Codex to run the task lifecycle, use the repo-local skill at
`codex/skills/codex-task-lifecycle/SKILL.md`.

That automation is Codex-native. It uses the phase sequence:
task -> design -> design review -> implementation -> review before PR -> PR ->
pr fixes -> done.
