Wrote `docs/designs/CLO-296-summary-json.md` with all seven sections: Problem, Goals/Non-goals, Architecture (modules + ASCII data-flow diagram + concrete types), Public API surface (real Rust signatures for `SummarySink`, `SummaryWriter`, `Summary`, `PriceTable`, etc.), Test plan (the 5 contracted cases in `tests/summary.rs` plus aggregate/prices unit coverage and schema-validation harness), Migration (nothing to migrate; additive), and 6 genuinely open questions (pricing-table location, `cost_budget_usd` plumbing, tri-state status, partial-pricing total semantics, manifest dedupe, run timestamp source-of-truth).
