Wrote design to `docs/designs/CLO-358-fix-manifest-kind.md`. Sections: Problem, Goals/Non-goals, Architecture (with ASCII diagram and mapping pseudocode), Public API surface (Rust signatures for `Kind`, `kind_from_filename`, and updated match sites), Test plan (named unit + integration tests), Migration/rollout (no data migration; schema relaxation only), and three Open questions left unresolved (schema strictness, case sensitivity, step-based reuse).
