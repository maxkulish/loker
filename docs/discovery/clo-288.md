# Discovery Report: CLO-288

**Task**: Author `design-doc-tdd` workflow file at `.lok/workflows/design-doc-tdd.toml`
**Phase**: Discovery
**Date**: 2026-05-03

---

## Problem Framing

**WHO is affected**: Workflow authors who want to run loker's canonical design-doc-tdd pipeline, and the M6 end-to-end integration test that validates the complete Slice A.

**WHAT today**: No `.lok/workflows/design-doc-tdd.toml` file exists. The CLO-287 grammar parser exists (merged in PR #35) but the canonical workflow file is still a hand-rolled placeholder in `tests/fixtures/workflows/design-doc-tdd.toml` with the wrong structure (uses phases `research`, `design`, `implement`, `review` instead of `design`, `review`, `implement`, `verify`).

**WHAT we want**: The correct four-phase workflow (`design → review → implement → verify`) matching PRD §UC-1, with proper strategies, backends, hooks, and topological inputs.

**WHY now**: Roadmap row T-034. CLO-287 (grammar parser) is complete and unblocks this. M6 integration test (T-037) depends on this file existing and matching the spec byte-for-byte.

---

## Existing Code

### 1. `.lok/workflows/` — existing workflows (3 files)

| File | Phases | Purpose |
|------|--------|---------|
| `design-draft.toml` | 5 steps | Claude-powered first draft of design doc |
| `design-review.toml` | 6 steps | Multi-model design document review |
| `implement-gate.toml` | 6 steps | Pre-PR validation gate (codex + gemini) |

**Key pattern**: These files use a `[[steps]]` structure (not `[[phases]]`). They are the old-generation "shell-script + prompt" workflows, not the new Rust-parsed phase-based workflows.

### 2. `tests/fixtures/workflows/design-doc-tdd.toml` — current placeholder fixture

Has 4 phases (`research`, `design`, `implement`, `review`) but:
- Wrong phase names (should be `design`, `review`, `implement`, `verify`)
- Uses old strategy syntax (e.g., `{ single = {} }` works, but `{ parallel = { min_responses = 2 } }` and `{ escalating = { pass_failure_context = true } }` need to match the grammar parser's enum variants)
- Missing hooks (`test_runner`, `run_command`, `llm_verifier`)
- Missing cost budget placeholder
- Missing top-of-file comment about backend swap points

### 3. `src/workflow/grammar.rs` — grammar parser (CLO-287)

The parser defines:
- `Strategy` enum: `Single`, `ParallelFanOut { min_responses: usize }`, `EscalatingRetry { pass_failure_context: bool }`
- `Phase` struct: `name`, `strategy`, `backends`, `prompt_template`, `inputs`, `output`, `contract`
- `Workflow` struct: `name`, `description`, `phases`, `defaults`
- Validation: forward input refs, unknown phase refs, strategy constraints, duplicate names
- `lint()`: warns about `phase.contract` (FR-31 reservation)

**Valid TOML strategy syntax**:
```toml
strategy = { single = {} }
strategy = { parallel = { min_responses = 2 } }
strategy = { escalating = { pass_failure_context = true } }
```

### 4. PRD §UC-1 (lines 63-72) — required phase specification

| Phase | Strategy | Backends | Output | Inputs |
|-------|----------|----------|--------|--------|
| `design` | `single` | tensorzero/design | `design.md` | `spec` |
| `review` | `parallel`, min_responses=2 | cross-family | `review.md` | `phase:design` |
| `implement` | `escalating_retry`, pass_failure_context=true | ollama/ < claude/ < codex/ | `changes/` | `phase:design`, `phase:review` |
| `verify` | `parallel`, min_responses=1 | any_fail aggregator | `verify.json` | `phase:implement` |

### Baseline Score: 8/10

The grammar parser is complete and well-tested. The main work is authoring the TOML file and replacing the placeholder fixture. No significant refactoring needed.

---

## Approaches Considered

### Approach A: Author the canonical file directly (single-file authoring)

**Summary**: Write `.lok/workflows/design-doc-tdd.toml` directly matching PRD §UC-1 byte-for-byte, then update `tests/fixtures/workflows/design-doc-tdd.toml` to be a copy.

**Pros**:
- Minimal work — just one file
- Clear alignment with PRD
- Directly unblocks M6 integration test

**Cons**:
- No validation until `make check` runs
- Hook types (`test_runner`, `run_command`, `llm_verifier`) need to be verified against the grammar — not yet clear if hooks are in the grammar

**Effort**: S | **Risk**: Low

### Approach B: Verify grammar hook support first, then author

**Summary**: Check if `src/workflow/grammar.rs` `Phase` struct supports hooks before authoring. May need to extend the grammar.

**Pros**:
- Catches grammar gaps early
- Ensures the file is actually parseable

**Cons**:
- May need to extend the grammar (CLO-287's scope may not have included hooks)
- More iteration

**Effort**: M | **Risk**: Medium

---

## Approach Chosen: A

Author the canonical file directly. The `Strategy` variants and core fields are fully implemented in the grammar. Hooks are mentioned in the PRD and Linear issue but may need a follow-up if the grammar doesn't yet support them — that's a follow-up debt item, not a blocker for the core file.

---

## Discovery Debt

| Debt | Action |
|------|--------|
| `phase.contract` FR-31 lint already working (verified in grammar.rs unit test) | No action needed |
| Hook types (`test_runner`, `run_command`, `llm_verifier`) — not yet confirmed if `Phase` struct has a `hooks` field | Check grammar.rs `Phase` struct; if missing, file the hook (pun intended) as CLO-follow-up |
| CLO-287's test fixture `tests/fixtures/workflows/design-doc-tdd.toml` uses wrong phase names and structure | This task replaces it; confirm round-trip test uses the new path `.lok/workflows/design-doc-tdd.toml` |

---

## Key Findings

1. **Grammar parser is complete** (CLO-287, PR #35 merged). Strategy variants: `single`, `parallel`, `escalating`.
2. **Old workflows** use `[[steps]]` shell-script format; new phase-based workflows use `[[phases]]` TOML array.
3. **Hand-rolled fixture exists** at `tests/fixtures/workflows/design-doc-tdd.toml` with wrong structure — must be replaced with the canonical file.
4. **PRD §UC-1 defines the exact four phases** needed: `design` → `review` → `implement` → `verify`.
5. **Hooks** (`test_runner`, `run_command`, `llm_verifier`) are referenced in the spec but may not be in the grammar yet — need verification.
6. **FR-31 reservation** (phase.contract) already has lint warning in grammar.rs — forward-compat test will pass.

---

## Next: Design Phase

Author the four-phase workflow file, verify it parses, and update the test fixture.