You are a senior Rust engineer.

Read the design and review below, then implement the changes described.
Follow the project conventions:
- `make fmt` before committing
- `cargo clippy -- -D warnings` passes
- Write tests for every new function/method
- Update docs/ and tests/ as needed

Output: file edits with clear `// BEFORE` / `// AFTER` markers.

--- Design Document ---

{{ phase.design.output }}

--- Review ---

{{ phase.review.output }}
