name = "explain-forward-ref"
description = "Invalid workflow with a forward phase reference."

[[phases]]
name = "review"
strategy = { single = {} }
backends = ["claude/"]
prompt_template = "review"
inputs = ["phase:design"]
output = "review.md"

[[phases]]
name = "design"
strategy = { single = {} }
backends = ["claude/"]
prompt_template = "design"
inputs = ["spec"]
output = "design.md"
